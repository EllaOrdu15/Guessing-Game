// In your main function, generate a random number.

// Initiate a infinite loop
  // Receive input from the user on the terminal.
  // Compare user input with random number.

  // If input < random number, print "Too low" and continue
  // If input > random number, print "Too high" and continue
  // If input == random number, print "You guessed it!" and break

// Print out "Good Bye from Developer Ella!"